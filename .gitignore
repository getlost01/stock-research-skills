.env
.env.local
node_modules/
*.log
.DS_Store

# Generated reports contain real portfolio/financial data — never commit
reports/

# Your real plan and its structured data live in .portfolio/ — holdings,
# fund folios, targets, tax and income figures. Never commit.
# Only the template (PORTFOLIO-PLAN.example.md) is tracked.
.portfolio/
PORTFOLIO-PLAN.md

# The skills ship from plugins/stock-research-skills/skills/ — anything
# that lands here is a local install or scratch copy, never the source.
.claude/skills/